# 项目规则

## Zep Cloud 知识图谱

本项目使用 Zep Cloud 知识图谱进行记忆管理。

- **graph_id**: `mirofish`
- 调用 Zep Cloud MCP 工具时，始终使用此 graph_id 参数
- 使用 `zep-cloud_add_graph_data` 添加记忆，使用 `zep-cloud_search_graph` 搜索记忆
- 支持添加 text/json/message/triple 类型数据
